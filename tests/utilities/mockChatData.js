export default function mockChatData(
    chatId,
    userId,
    name,
    messages,
    more,
    friends
) {
    return {
        chatId,
        userId,
        name,
        messages,
        more,
        friends,
    };
}
